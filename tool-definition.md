# 🧰 도구 정의서 — 선박기기 매뉴얼 RAG 챗봇

> 모델이 호출할 수 있는 도구의 **입력·출력·책임·실패 행동**을 양식에 맞춰 확정한 명세.
> [`design-packet.md`](design-packet.md) §②의 읽기 도구 4개를 상세화하고, §③의 상태 변경 동작(색인 덮어쓰기·외부 전송 전환)을 도구로 정식화한다. 코드 근거: `app.py`.
>
> 작성일: 2026-08-29 · 상태: 초안(v1)

## 공통 규약 (전 도구)

- 기본은 **로컬 읽기**, 외부 통신 없음. 유일한 예외는 `switch_generation_backend`(승인 후).
- 실패는 예외를 던지지 않고 `status`/오류 코드로 반환한다.
- **내부 식별자**(임베딩, `index.npz` 배열 인덱스, 절대경로, 청크 내부 `id`)는 **도구 간 연계용일 뿐**, 사용자 답변에는 노출하지 않는다(§ 반환 형식 공통 규칙).
- 임계값을 **몰래 낮춰** 저신뢰 근거를 확정처럼 제시하지 않는다.

## 🔀 도구 선택 분기 (‘쓰는 때’ 겹침 방지)

네 읽기 도구는 반환물이 배타적이라 겹치지 않지만, 모호한 요청은 아래로 분기한다.

- **"…있어?/무슨 매뉴얼?"** → `list_manual_sets` (세트 자체를 물음)
- **"…어떻게 해/절차/사양"** → `search_manual` (내용 근거 검색)
- **"그 도면 보여줘/확대"** → 직전 검색이 준 참조가 있으면 `get_page_asset`, 없으면 먼저 `search_manual`
- **"이거 정확해?/믿을 만해?"** → `search_manual` 이후 `get_evidence_flags` (신뢰도만 판정)

> 원칙: 한 도구의 ‘쓰는 때’가 다른 도구와 겹치면 **책임을 줄이거나(참조만/실체만) 사용자에게 한 번 되물어** 분기한다.

---

## 읽기 도구 (상태 조회 · 자동 허용)

### 1. `list_manual_sets` — 색인된 매뉴얼 **세트 목록을 조회한다**

| 항목 | 내용 |
|---|---|
| **쓰는 때** | 대화 시작·세트 미선택 시, "어떤 매뉴얼 있어?" 질문 시, 세트 선택 UI를 채울 때 |
| **안 쓰는 때** | 세트가 이미 정해져 **내용 검색**이 목적일 때(→ `search_manual`) · 세트 *내부* 를 찾을 때 |

- **인자 스키마**

  | 인자 | 자료형 | 필수 | 허용값 | 누락 시 |
  |---|---|---|---|---|
  | `name_filter` | string | ⬜ | 임의 문자열 | 전체 세트 반환 |

- **반환 형식**: `{ sets:[{ name, has_text_index:bool, doc_count?:int }], status:"ok" }` · 오류 코드 `DIR_READ_FAILED`
  - 답변 인용 최소 필드: **세트 이름**(과 색인 유무). 경로·파일 목록 원본은 넘기지 않는다.
- **실패 뒤 행동**: 빈 목록 → **재시도 없음**, "색인 세트가 없습니다 — ‘색인 생성’ 탭에서 먼저 만드세요"로 **보류**. `DIR_READ_FAILED` → **1회 재시도**, 실패 시 **운영자 이관**.

### 2. `search_manual` — 질의에 대한 **근거 청크·이미지 참조를 검색한다** (답 문장 생성 안 함)

| 항목 | 내용 |
|---|---|
| **쓰는 때** | 세트 선택 후 사용자 질의마다 1회(멀티턴이면 현재 질의로) |
| **안 쓰는 때** | 세트 미선택(먼저 세트 선택 요청) · 이미지 **열람**이 목적(→ `get_page_asset`) · 신뢰도만 확인(→ `get_evidence_flags`) · 세트 목록 확인(→ `list_manual_sets`) |

- **인자 스키마**

  | 인자 | 자료형 | 필수 | 허용값 | 누락 시 |
  |---|---|---|---|---|
  | `query` | string | ✅ | 자연어(한글 가능) | 호출하지 않고 질문을 요청 |
  | `set_name` | string | ✅ | 존재하는 세트명 | `SET_NOT_SELECTED` |
  | `text_threshold` | float | ⬜ | 0.0–1.0 | 0.5 (`TEXT_THRESHOLD_DEFAULT`) |
  | `img_threshold` | float | ⬜ | 0.0–1.0 | 기본값 |
  | `k_text` / `k_img` | int | ⬜ | ≥1 | 5 / 4 (`TOP_K=5`) |

- **반환 형식**: `{ text_hits:[{ tag, chunk, score, page_ref, id }], image_refs:[{ ref, caption, score }], checked_at, status:"ok|empty" }` · 오류 코드 `SET_NOT_SELECTED` · `INDEX_MISSING` · `LOAD_FAILED`
  - 답변 인용 최소 필드: `tag`(파일·p쪽) · `url`(파일#page) · `score` · **적용 조건**(대상 기기/절차, 저신뢰 여부) · `checked_at`(확인 시각). `id`·점수 배열·임베딩은 **연계용/비노출**.
- **실패 뒤 행동**: `empty`(근거 없음) → **재시도 없음**(임계값 임의 인하 금지), 사용자에게 *질의 재구성·세트 변경·임계값 조정* 여부를 **질문**; 반복되면 **정비·기술지원 이관**. `SET_NOT_SELECTED` → 세트 선택 **질문**. `INDEX_MISSING` → 재색인 안내(**보류**), 반복 시 **운영자 이관**. `LOAD_FAILED` → **1회 재시도** 후 운영자 이관.

### 3. `get_page_asset` — 검색이 돌려준 **참조 하나의 이미지(페이지/크롭)를 연다**

| 항목 | 내용 |
|---|---|
| **쓰는 때** | 사용자가 특정 도면·페이지를 "보여줘/확대"할 때, 1순위 답의 근거 페이지 그림을 인라인 제시할 때 |
| **안 쓰는 때** | 아직 검색 결과가 없을 때(→ 먼저 `search_manual`) · 검색이 준 적 없는 **임의 경로** 요청(거부) · 텍스트 근거 검색이 목적일 때 |

- **인자 스키마**

  | 인자 | 자료형 | 필수 | 허용값 | 누락 시 |
  |---|---|---|---|---|
  | `set_name` | string | ✅ | 존재하는 세트명 | 오류 |
  | `ref` | string | ✅ | **직전 검색이 준** `ref`/`page_ref` 값만 | 미확인 경로면 `ASSET_NOT_FOUND`(거부) |

- **반환 형식**: `{ image_path, caption, kind:"page"|"crop", status }` · 오류 코드 `ASSET_NOT_FOUND` · `ASSET_BROKEN`
  - 답변에는 **캡션(출처·유사도)** 만. 절대경로는 상대 참조로 축약.
- **실패 뒤 행동**: `ASSET_NOT_FOUND`(임의 경로) → **재시도 없음**, `search_manual`로 유효 참조 확보 **분기**. `ASSET_BROKEN`(손상) → **1회 재시도**, 실패 시 이미지 **보류**하고 텍스트 근거로 답 유지(PRD §6).

### 4. `get_evidence_flags` — 청크의 **검증 플래그·section·중복 여부를 조회한다**

| 항목 | 내용 |
|---|---|
| **쓰는 때** | 검색 점수가 임계값 부근·스캔본(OCR)일 때, 답에 ‘저신뢰’ 라벨/원문 확인 문구를 붙일 근거로, "믿을 만해?" 질문에 |
| **안 쓰는 때** | 청크 **본문**이 필요할 때(그건 `search_manual`의 `chunk`) · 이미지 관련 · 검색 전 |

- **인자 스키마**

  | 인자 | 자료형 | 필수 | 허용값 | 누락 시 |
  |---|---|---|---|---|
  | `set_name` | string | ✅ | 존재하는 세트명 | 오류 |
  | `chunk_ids` | string[] | ✅ | `search_manual`이 준 `id` | 호출하지 않음 |

- **반환 형식**: `{ flags:[{ id, section, flags:["120자_미만_유지"|"section_미확인"|"OCR_저신뢰"], duplicate_of? }], status }` · 오류 코드 `REPORT_MISSING`
  - 답변에는 **‘저신뢰/검증 필요’ 여부와 `section`** 만 반영. `id`는 비노출.
- **실패 뒤 행동**: `REPORT_MISSING`(구버전 색인) → **재시도 없음**, "플래그 정보 없음 → 보수적으로 **검증 필요**로 안내"(보류적 라벨). 없는 `id` → 생략.

---

## 상태 변경·외부 전송 도구 (실행 전 사람 확인/승인 · 챗봇 단독 결정 금지)

> 아래 두 도구는 **에이전트가 제안·준비**할 수는 있으나, **실행은 사람의 확인/승인 이후**에만 한다.

### 5. `rebuild_index` — 매뉴얼 PDF를 **색인 생성/덮어쓰기 한다** ⚠️ 코퍼스 변경

| 항목 | 내용 |
|---|---|
| **쓰는 때** | 사용자가 새 매뉴얼 추가·재색인을 요청하고 **명시적으로 확인**했을 때(기본은 ‘색인 생성’ 탭에서 사람이 수행) |
| **안 쓰는 때** | **덮어쓰기 확인 없이** 자동 실행 금지(기존 색인 파괴 위험) · 조회/검색 목적 · 챗봇이 스스로 판단해 실행 금지 |

- **인자 스키마**

  | 인자 | 자료형 | 필수 | 허용값 | 누락 시 |
  |---|---|---|---|---|
  | `files` | list | ✅ | PDF 경로 | 오류 |
  | `set_name` | string | ✅ | 세트명 | 오류 |
  | `mode` | enum | ✅ | `append`(추가) · `overwrite`(덮어쓰기) | **보류하고 질문**(어느 쪽인지) |
  | `ocr_conf` 등 | float | ⬜ | 0.0–1.0 | 기본값 |

- **반환 형식**: `{ set_name, added_count, report_path("verify_report.md"), status }` · 오류 코드 `BUILD_FAILED`
- **실패 뒤 행동**: 실행 전 **사용자 확인 미획득** → 실행 안 함(**보류**); `overwrite`는 특히 재확인. `BUILD_FAILED`(부분 색인 위험) → **자동 재시도 없음**, **운영자 이관**.
- **권한**: 쓰기 — **실행 전 사용자 확인 필수**.

### 6. `switch_generation_backend` — 생성 백엔드를 **로컬↔외부 API로 전환한다** ⚠️ 외부 전송(정책 예외)

| 항목 | 내용 |
|---|---|
| **쓰는 때** | 사용자가 API 경로를 원하고, 키 입력 + **명시적 ‘외부 전송 동의’** 를 승인했을 때만 |
| **안 쓰는 때** | 승인·경고 없이 전환 금지 · 보안 매뉴얼 세트 · **챗봇 단독 전환 절대 금지**(전환 순간 매뉴얼이 외부로 나감) |

- **인자 스키마**

  | 인자 | 자료형 | 필수 | 허용값 | 누락 시 |
  |---|---|---|---|---|
  | `target` | enum | ✅ | `local` · `api` | 오류 |
  | `api_base` / `api_model` | string | `api`일 때 ✅ | OpenAI 호환 base URL·모델명 | `MISSING_API_FIELDS` |
  | `api_key` | string | `api`일 때 ✅ | **세션 메모리 전용**(로그·디스크 금지) | `MISSING_API_FIELDS` |

- **반환 형식**: `{ active_backend, external_egress:bool, warning_ack:bool, status }` · 오류 코드 `MISSING_API_FIELDS` · `LLM_CONN_FAILED`
  - 키·API 원응답은 **절대 반환/로그 금지**.
- **실패 뒤 행동**: 승인/경고 미확인 → 전환 안 함(**로컬 유지·보류**). `MISSING_API_FIELDS` → base/키/모델 재입력 **질문**. `LLM_CONN_FAILED` → **1회 재시도** 후 로컬 복귀 안내(**보류/이관**).
- **권한**: 쓰기·외부 전송 — **명시 승인 + 전송 경고 필수, 챗봇 단독 결정 금지**.

---

## 🔐 권한 표

| 도구 | 종류 | 상태 영향 | 승인 |
|---|---|---|---|
| `list_manual_sets` | 조회 (R) | 읽기 | 자동 허용 |
| `search_manual` | 조회 (R) | 읽기 | 자동 허용 |
| `get_page_asset` | 조회 (R) | 읽기 | 자동 허용 |
| `get_evidence_flags` | 조회 (R) | 읽기 | 자동 허용 |
| `rebuild_index` | 변경 (W) | 코퍼스 변경 | **실행 전 사용자 확인 필수** |
| `switch_generation_backend` | 변경 (W)·외부 전송 | 매뉴얼 외부 유출 | **명시 승인 + 전송 경고 · 챗봇 단독 금지** |

**챗봇이 단독으로 결정하지 않는 것** (사람 확인 또는 담당자 승인 필수):

- **색인 덮어쓰기 확정** — 기존 색인을 파괴하는 되돌릴 수 없는 변경 *(cf. 예약 확정)*
- **외부 전송 정책 예외** — 로컬→API 전환으로 매뉴얼이 외부 업체로 전송됨 *(cf. 정책 예외 승인)*
- **자격증명·민감정보 열람/입력** — API 키 등은 세션 메모리 전용, 로그·디스크 금지 *(cf. 개인정보 열람)*

> 본 프로젝트는 개인정보를 저장·처리하지 않는다(로컬·무계정). 대신 **API 키**와 **독점 매뉴얼 내용의 외부 노출**을 같은 등급으로 통제한다.

## 📦 반환 형식 공통 규칙

**답변에 넣는 것** — 근거 **출처(파일·페이지)** · **URL(`파일#page`)** · **적용 조건**(대상 기기/절차, 저신뢰 여부) · **확인 시각**(`checked_at`) · (선택) 색인 세트/버전.

**넣지 않는 것** — 검색 결과 **전체 덤프**(청크 원문 전부·이미지 전부), **점수 배열·임베딩**, **내부 식별자**(청크 내부 `id`·`index.npz` 배열 인덱스·절대경로), **키·API 원응답**.

> 발췌는 **최대 300자**([`design-packet.md`](design-packet.md) §⑤ 및 `_build_related_md`와 일치). 인용은 실제 사용한 근거만(장식용 출처 금지).
