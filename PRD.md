# 📝 PRD — 선박기기 매뉴얼 RAG 안내 챗봇

> 선박기관사를 위한 **로컬 우선 · 근거 기반(RAG) 생성형 질의응답** 챗봇.
> 검색 파이프라인(`app.py`)을 **"검색 → 근거 기반 답변 생성(Generation)"** 으로 확장한다.
>
> 작성일: 2026-08-28 · 상태: 핵심 기능 구현됨

---

## 0. 문서 개요

- **목적**: 기존 검색 파이프라인 위에 **근거 기반 문장 답변 생성 레이어**와 **챗 UI**를 얹어, 선박기관사가 자연어로 물으면 매뉴얼 근거로 답하고 출처·그림을 함께 제시하는 챗봇의 요구사항을 정의한다.
- **범위(In-scope)**: 답변 생성(폐쇄형 RAG), 출처 인라인 인용, 1·2순위 결과 노출, 그림 열람, 대화 모드 토글, 로컬/API 전환 설정.
- **범위 밖(Out-of-scope)**: 색인 파이프라인 자체의 재설계(현재 것을 계약으로 사용), 정비 기록(로그) 통합, 도메인 파인튜닝 — 이들은 향후 로드맵 항목.
- **핵심 용어**
  - **RAG**: 검색으로 찾은 근거를 바탕으로 답을 생성하는 방식.
  - **폐쇄형(closed-book)**: 제공된 매뉴얼 근거 밖의 지식은 쓰지 않는 답변 정책.
  - **세트(set)**: 매뉴얼 묶음 단위 색인(`indexes/<세트이름>/`).
  - **청크(chunk)**: 검색·표시 단위로 쪼갠 본문 조각.
  - **TTFT(Time-To-First-Token)**: 질의 후 답변 첫 토큰(첫 문장)까지 걸리는 체감 응답 시간.

---

## 1. 배경 · 문제 정의

선박기관사는 기기 상태를 판단·조치할 때 매뉴얼·기록에 의존하는데, 종이/PDF 매뉴얼을 목차부터 페이지까지 수작업으로 찾는 데 시간이 든다. 보안상 외부 통신이 제한되는 경우가 많아 **로컬 동작**이 중요하다.

검색 파이프라인(`app.py`)은 **검색(Retrieval) 단계**를 담당한다 — 결과(도면·표 이미지 + 본문 청크)를 나열한다. 본 PRD는 그 위에 **"검색된 근거로 문장 답변을 생성(Generation)"** 하는 단계를 더해 RAG를 완성한다.

---

## 2. 목표 / 비목표

### 2.1 목표

- 사용자의 질문에 대해 **색인된 매뉴얼 근거 안에서** 사용자 의도에 맞는 답을 **빠르게 생성**한다.
- 답변에 **출처(파일명 + 페이지)** 를 인라인으로 인용해 원문에서 즉시 검증 가능하게 한다.
- 관련 **그림(도면·표)** 을 함께 제시하고 클릭하면 크게 볼 수 있게 한다.
- **1순위 답**을 우선 노출하되, **연관(2순위 이하) 자료**는 클릭으로 펼쳐 볼 수 있게 한다.

### 2.2 비목표 / 제약

- **유료 API 기본 미사용.** **로컬 LLM이 기본** 경로다.
- 단, **API 대체 경로**를 위한 **키 입력 설정칸**을 둔다. 사용자가 키를 입력하고 **수동으로 전환**할 때만 API를 사용한다(§5 FR-6, §7 NFR-1).
- **[보안 제약]** API 경로를 켜는 순간 **검색된 매뉴얼 내용이 외부 업체로 전송**된다. 따라서 API는 **기본 비활성**이며, 전환 시 **명시적 경고**를 표시한다. 실제 보안 매뉴얼은 로컬/사내망 경로로만 사용함을 원칙으로 한다.
- 색인 파이프라인·모델 파인튜닝·기록 통합은 본 PRD 범위 밖(향후 로드맵).

---

## 3. 사용자 · 시나리오

- **주 사용자**: 선박기관사(현장/기관실), 정비·기술지원 엔지니어.
- **환경**: 오프라인 또는 사내망(LAN). 외부 통신 제한 가능.

**대표 시나리오**

1. *절차 확인* — "청정기 분해 절차 알려줘" → 근거 청크로 답변 생성 + `[출처 파일 · p쪽]` + 해당 페이지 그림.
2. *교차언어 조회* — 한글로 물어도 영문 매뉴얼 본문이 근거로 매칭(bge-m3 다국어).
3. *도면 열람* — "냉각수 배관도" → 관련 도면 크롭을 답과 함께 노출, 클릭 시 확대.
4. *근거 없음* — 색인 밖 질문 → "매뉴얼에서 근거를 찾지 못했습니다"로 응답(지어내지 않음).

---

## 4. 데이터 소스 & 인덱스 계약

생성 레이어는 기존 색인 산출물(`app.py`가 만든 것)을 **계약(contract)** 으로 사용한다. 아래는 코드에서 확인된 사실이다.

- **저장 구조**: `indexes/<세트이름>/{index.npz, crops/, pages/}`
- **`index.npz` 배열**: `embeddings`(이미지), `filenames`, `image_paths`, `kinds`, `chunk_texts`, `chunk_labels`, `chunk_pages`, `text_embeddings`
- **출처 메타데이터**: 청크 라벨 = `"{파일명} · p{페이지}"` → **파일명 + 페이지 단위**로 확보됨(절/조항/그림번호 단위는 없음).
- **이미지**: 페이지 전체 렌더(`pages/`, 150 DPI) + 도면·표 크롭(`crops/`, DocLayout-YOLO 검출).
- **임베딩 모델**: 이미지 = SigLIP2(`google/siglip2-base-patch16-224`), 텍스트 = bge-m3(`BAAI/bge-m3`, 1024차원, 다국어/교차언어).
- **검색**: 코사인 유사도 상위 `TOP_K=5`, 임계값 게이트. 텍스트는 페이지당 최고 청크 1개.
- **OCR**: 스캔본은 RapidOCR → EasyOCR(ko+en) 폴백.

**계약상 한계**

- 본문 청크와 그림은 **같은 페이지 여부**로만 연결된다(문단-그림 정밀 연결 없음).
- 청킹은 **문단·문장 경계 기반 근사**(사실 단위 보장은 아님 — §4.1).
- 교차언어 코사인 점수대가 낮아(대략 0.5~0.6) 임계값 튜닝이 필요.
- `indexes/`는 매뉴얼을 색인해야 채워진다(초기 상태는 비어 있음).

### 4.1 청크 산출물 규격 (색인 시 함께 생성) ✅ 구현됨

색인·청킹 결과를 아래 규칙에 맞춰 세트 폴더에 **구조화 산출물**로 내보낸다.

**필드 — `indexes/<세트>/chunks.jsonl` (청크당 1줄 JSON)**

| 필드 | 값 | 비고 |
|---|---|---|
| `id` | `<파일>_p<쪽>_c<번호>` | 자동 생성·고유 |
| `text` | 원문에서 **그대로 추출한** 사실 단위 | 가공·추론 없음(규칙 5) |
| `url` | `<원본파일명>#page=<쪽>` | 로컬 PDF 페이지 앵커(결정 ①) |
| `section` | PDF 북마크/목차의 해당 섹션, 없으면 `[검증 필요]` | `get_toc()` 기반(결정 ②) |
| `source_file`·`page`·`char_len`·`ocr`·`flags` | 지원 필드 | 규칙 3·6 근거 |

**규칙 반영**
- **최소 120자·사실 단위(1)**: 문단(빈 줄) 경계=주제 경계로 보고 **그 안에서만** 문장을 병합해 120자↑를 목표(소프트 최소치, `CHUNK_MIN_CHARS`). 문단이 짧아 미달이면 **병합하지 않고 유지 + `120자_미만_유지` 플래그**(규칙 4·5 우선).
- **미확인 표기(3)**: 채우지 못한 필드는 `[검증 필요]`, 저신뢰 근거는 플래그(`section_미확인`·`OCR_저신뢰`).
- **주제 분리(4)**: 문단 경계를 넘겨 병합하지 않음(자동 근사 — 완전 보장 아님).
- **원문 외 추가 금지(5)**: `text`는 verbatim, 메타는 사실 기반만.
- **검증 리포트(6) — `indexes/<세트>/verify_report.md`**:
  - **중복 가능 청크**: 청크 임베딩 코사인 ≥ `DUP_COSINE`(기본 0.95) 쌍 목록
  - **사람이 원문 확인 필요**: 플래그별 청크 id 목록

> 한계: "사실 단위/주제 분리"는 북마크·문단 구조에 기댄 **자동 근사**다. 그래서 `verify_report.md`의 사람 확인 목록이 안전장치다.

---

## 5. 기능 요구사항 (FR)

### FR-1. 근거 기반 답변 생성 (폐쇄형)
> ✅ 구현됨 — `app.py`: `chat_respond`·`ollama_chat_stream`·`build_gen_messages`
- 검색 상위 근거 청크를 **로컬 LLM(Ollama `qwen3.5:2b`)** 에 전달해 **문장 답변을 생성**한다.
- **폐쇄형 원칙**: 제공된 근거 밖의 지식은 사용하지 않는다. 근거가 없으면 **"모름"** 으로 답한다(§6).
- 생성은 **스트리밍**으로 출력한다(체감 지연 완화).

### FR-2. 출처 인라인 인용
- 답변의 문장/문단에 근거를 **`[출처 파일이름 · p12]`** 형식으로 인라인 표기한다(파일명 + 페이지).
- 인용은 실제 사용된 근거 청크와 연결되어야 한다(장식용 출처 금지).

### FR-3. 1순위 답 / 2순위 연관
- **1순위 답**: 최상위 근거로 생성한 답변(인라인 인용 + 가장 관련된 그림 1개 인라인).
- **2순위/연관**: 답에 사용되지 않은 **나머지 상위 검색 결과**(관련 페이지·그림)를 **클릭 시 펼쳐** 볼 수 있게 한다(기존 top-5 검색 결과가 이 자리로 이동).

### FR-4. 그림 클릭 열람
- 답의 **근거 페이지에 있는 그림**을 관련 그림으로 노출한다.
- 썸네일 클릭 시 확대 열람(기존 Gradio 갤러리 확대 기능 활용).

### FR-5. 대화 모드 토글
> ✅ 구현됨 — 챗 탭의 '멀티턴(대화 맥락 기억)' 체크박스
- 기본은 **단발형 Q&A**(질문마다 독립).
- **버튼 하나로 멀티턴 모드로 전환**할 수 있게 한다(대화 맥락 유지). 멀티턴 세부 동작은 §11 오픈 이슈.

### FR-6. 로컬 / API 전환 및 키 설정칸
> ✅ 구현됨 — OpenAI 호환 API(base URL·모델명·키), 키는 세션 메모리에서만 사용
- **기본 = 로컬 LLM (Ollama `qwen3.5:2b`).**
- 설정에 **API 키 입력칸**을 둔다. 키 입력 + **명시적 "API 사용" 선택** 시에만 API 경로로 전환(수동).
- 전환 시 **"매뉴얼 내용이 외부로 전송됨" 경고**를 표시한다(§7 NFR-1).

---

## 6. 답변 정책 (폐쇄형 grounding)

LLM에 "제공된 근거만 사용하고, 없으면 모른다고 답하라"를 강제한다. 상황별 처리:

| 상황 | 처리 |
|---|---|
| **도메인 밖 / 검색 hit 없음** | 답변 생성 금지. "매뉴얼에서 근거를 찾지 못했습니다" + 세트 변경·재질문 유도 |
| **근거 약함(임계값 부근)** | 지어내지 않음. **저신뢰 후보를 '저신뢰'로 명시**해 출처와 함께 노출, 최종 판단은 사람 |
| **판정 실패(OCR 깨짐·그림 미검출)** | 텍스트가 실패해도 **원본 페이지 이미지를 그대로** 제시. 한쪽 모달이 비어도 다른 쪽은 표시 |
| **안전 직결 절차** | 챗봇은 **"참고·검색 보조"** 임을 명시. 최종 판단은 **면허 기관사 + 원문 페이지 확인**에 위임 |

---

## 7. 비기능 요구사항 (NFR)

### NFR-1. 로컬 우선 · 보안
- 기본 동작은 **완전 로컬**(외부 통신 없음). 모델 가중치는 최초 1회 다운로드 후 오프라인.
- 로컬 LLM은 **Ollama 서버**(기본 `http://localhost:11434`)로 호출한다 — **로컬 프로세스 간 통신**이며 외부로 나가지 않는다. 모델은 `ollama pull qwen3.5:2b`로 최초 1회 받은 뒤 오프라인 동작.
- API 경로는 **기본 비활성 · 수동 전환 · 전송 경고** 필수. 키 저장 방식(메모리 전용 vs 디스크 저장)은 §11에서 확정.

### NFR-2. 성능
- **검색 단계**와 **생성 단계**를 분리해 측정한다.
- **검색**: 하드웨어 영향이 작음 → 초기 응답 목표(참고: 30초 이내)를 검색 단계에서 유지 가능 여부로 검증. 목표치는 `[측정 후 확정]`.
- **생성**: 실행 환경 편차가 크므로 단일 합격선 대신 **스트리밍 TTFT(첫 문장까지 시간)** 를 체감 지표로 삼고, 전체 완료 시간은 **환경별(GPU/MPS/CPU) 기록**한다. 목표치는 `[측정 후 확정]`(수치는 본 문서에서 지정하지 않음).

### NFR-3. 실행 환경
- **임베딩·검색 모델**(SigLIP2·bge-m3): 기존처럼 **CUDA / Apple MPS / CPU 자동 감지**(in-process transformers). 고정 대상 하드웨어를 두지 않는다.
- **로컬 LLM**(Ollama `qwen3.5:2b`, 약 2.7GB): **Ollama가 GPU/CPU를 자체 선택**한다. 소형 모델이라 CPU에서도 구동 가능(생성 속도는 §7.2에서 환경별 측정).

---

## 8. UX / UI

- **챗 인터페이스** ✅ 구현됨 — 새 탭 **'💬 챗봇 (RAG)'** 추가(기존 색인/검색 탭 유지). 답변(인라인 인용) + '연관 근거·그림' 아코디언 + '고급·생성 백엔드' 아코디언 구성.
- **답변 영역**: 생성 답변(인라인 인용) + 관련 그림(인라인, 클릭 확대) + "연관 자료 더보기"(2순위 이하 펼침).
- **대화 모드 토글 버튼**(단발형 ↔ 멀티턴).
- **설정**: 색인 세트 선택, 임계값(이미지/텍스트), **API 키 입력칸 + 로컬/API 전환 스위치(경고 포함)**.

---

## 9. 평가 · 수용 기준

수용 기준을 측정 가능한 형태로 정리한다. **공개 자료(색인 DB)만으로 검증 가능한지**를 함께 표기한다.

| # | 수용 기준 | 검증 방법 | 공개 자료만으로 검증 |
|---|---|---|---|
| AC-1 | 질문에 대해 DB 근거로 **답한다** | 검색 hit + 생성 답변 확인 | **부분** — 검색 hit은 라벨셋 있으면 가능하나, **답의 정확성은 전문가 정답셋 필요** |
| AC-2 | **출처(파일 + 페이지)** 표시 | 인용이 실제 근거와 일치하는지 확인 | **가능**(파일명·페이지는 인덱스에 저장됨) |
| AC-3 | **그림 클릭 열람** | 갤러리 확대 동작 | **가능**(기능) / **불가**(그 답에 맞는 그림인지 — 이미지 정답셋 없음) |
| AC-4 | **1순위 답 + 2순위 연관** | 노출·펼침 동작 | **가능**(동작) / **불가**(1순위가 최선인지 — 랭킹 정답셋 없음) |

**품질 축**

- **검색 품질**: 질의-정답 라벨셋을 구축해 **텍스트 hit@k** 측정(이미지 검색 정확도 정량화 포함). → 평가셋 **구축 필요**.
- **생성 품질**: **사실성·무환각·"모름" 정확도**(근거 없을 때 모른다고 답하는 비율) — **전문가 정답셋 필요(현재 없음)**.
- **재현성**: 실제 매뉴얼은 비공개 → 공개 재현용 **샘플 매뉴얼셋 별도 구축 필요**.

---

## 10. 위험 · 한계

| 구분 | 내용 |
|---|---|
| 로컬 생성 지연 | 자동 감지 환경(특히 CPU)에서 생성이 느릴 수 있음 → 스트리밍·환경별 목표로 대응 |
| API 보안 | API 경로는 매뉴얼 내용을 외부로 전송 → 기본 차단·경고로 대응 |
| 그림 연결 정밀도 | 본문-그림이 페이지 단위로만 연결(문단 단위 아님) |
| 근거 품질 | 교차언어 점수 낮음, 글자 수 청킹, OCR 의존(스캔 품질에 취약) |
| 평가 한계 | 정답셋 부족(이미지·생성 답변·교차모달) |

---

## 11. 오픈 이슈 / 미확정 (구현·측정 단계에서 확정)

- **로컬 LLM 모델**: ✅ Ollama `qwen3.5:2b`(멀티모달, 약 2.27B / 2.7GB, Apache-2.0)로 확정. v1은 **텍스트 근거 합성만** 사용, 그림 직접 판독(비전 입력)은 향후 검토.
- **API 대체 경로**: ✅ **OpenAI 호환**(base URL·모델명·키 입력)으로 구현. 키는 **세션 메모리 전용**(디스크 미저장), 특정 벤더 고정 없음.
- **성능 목표 수치** `[측정 후 확정]` — 검색 목표, 생성 TTFT/완료 시간(환경별).
- **멀티턴 세부 동작** — 맥락 유지 범위, 초기화 방식.
- **`section` 정밀도** — PDF 북마크가 있으면 채우고, 없으면 `[검증 필요]`(구현됨, §4.1). 헤딩 자동 검출(DocLayout-YOLO title 블록 등)은 향후.
- **평가셋 구축** — 이미지·생성 답변·교차모달 정답 라벨.

---

## 부록 A. 참고
- `app.py` — 색인/검색 파이프라인(인덱스 계약의 근거)
